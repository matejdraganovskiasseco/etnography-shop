/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './src/pages/**/*.{js,ts,jsx,tsx,mdx}',
    './src/components/**/*.{js,ts,jsx,tsx,mdx}',
    './src/app/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        border: "hsl(var(--border))",
        input: "hsl(var(--input))",
        ring: "hsl(var(--ring))",
        background: "hsl(var(--background))",
        foreground: "hsl(var(--foreground))",
        primary: {
          DEFAULT: "#8B0000", // Dark red from Etnography logo
          foreground: "#FFFFFF",
        },
        secondary: {
          DEFAULT: "#000000", // Black from logo
          foreground: "#FFFFFF",
        },
        accent: {
          DEFAULT: "#A52A2A", // Lighter red accent
          foreground: "#FFFFFF",
        },
        destructive: {
          DEFAULT: "hsl(var(--destructive))",
          foreground: "hsl(var(--destructive-foreground))",
        },
        muted: {
          DEFAULT: "#333333", // Dark gray for muted elements
          foreground: "#CCCCCC",
        },
        popover: {
          DEFAULT: "hsl(var(--popover))",
          foreground: "hsl(var(--popover-foreground))",
        },
        card: {
          DEFAULT: "#1A1A1A", // Dark card background
          foreground: "#FFFFFF",
        },
        etnography: {
          red: "#8B0000",
          black: "#000000",
          darkgray: "#1A1A1A",
          lightgray: "#CCCCCC",
        },
      },
      borderRadius: {
        lg: "var(--radius)",
        md: "calc(var(--radius) - 2px)",
        sm: "calc(var(--radius) - 4px)",
      },
    },
  },
  plugins: [],
}
